<h1>
    <%= title %>
</h1>
<% if (messages.notice) { %>
  <div class="alert alert-success form-success">
    <p class="mb-2"><strong>Success:</strong></p>
    <p class="mb-0"><%= messages.notice %></p>
  </div>
<% } %>

<% if (errors && errors.length > 0) { %>
  <div class="alert alert-danger form-errors">
    <p class="mb-2"><strong>Please fix the following errors:</strong></p>
    <ul class="mb-0">
      <% errors.forEach(error => { %>
        <li><%= error.msg %></li>
      <% }) %>
    </ul>
  </div>
<% } %>
                <div class="addClass-container">
                    <form id="adddClassForm" name="classification_name" action="/inv/add-classification" method="post">
                        <span><em>NAME MUST ME ALPHABETIC CHARACTERS ONLY</em></span>
                        <input type="text" name="classification_name" placeholder="classification" required
                            value="<%= typeof classification_name !== 'undefined' ? classification_name : '' %>" />
                        <button type="submit">Add</button>
                    </form>
                </div>